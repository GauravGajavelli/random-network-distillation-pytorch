# CUDA-enabled requirements for gebru (8x Quadro RTX 6000, driver 590.48.01).
#
# PyTorch: install the CUDA wheel matching the installed toolkit.
# Run `nvcc --version` to get the toolkit version, then pick the right line below.
# The driver supports up to CUDA 13.1; use the highest toolkit version available.
#
# Uncomment exactly ONE of the torch lines:

# CUDA 12.6 (safe fallback — widely available wheels):
# torch==2.12.0+cu126 --extra-index-url https://download.pytorch.org/whl/cu126

# CUDA 12.8:
# torch==2.12.0+cu128 --extra-index-url https://download.pytorch.org/whl/cu128

# If PyTorch 2.12.0 is not yet available for your CUDA version, install the
# latest available release:
#   pip install torch --extra-index-url https://download.pytorch.org/whl/cu126
# Check https://pytorch.org/get-started/locally/ for the current matrix.

# All other deps are the same as the root requirements.txt
gymnasium==1.3.0
minigrid==3.0.0
gymnasium-robotics==1.3.0
numpy==2.4.6
tensorboard
tensorboardX
imageio
matplotlib
